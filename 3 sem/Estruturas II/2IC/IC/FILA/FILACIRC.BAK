#include<iostream.h>
#include<conio.h>
#include "filacirc.h"
#include "llccc.h"

void main()
{
	int x=-1;
	fila_circular f;
	inicializa(f);
	clrscr();
	while(x!=0)
	{
		cout<<"Digite um valor para adicionar a lista: ";
		cin>>x;
		if(x!=0)
			insere(f,x);
	}
	imprime(f.L);
	getch();
        finaliza(f.L);
}